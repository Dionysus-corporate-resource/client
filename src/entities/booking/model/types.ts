export interface TableData {
  id: string;
  customer: string;
  culture: string;
  loadingPoint: string;
  unloadingPoint: string;
  volume: string;
  distance: string;
  view: number;
  rate: number;
}
